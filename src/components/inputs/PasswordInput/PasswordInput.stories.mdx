import { Meta, Story } from '@storybook/addon-docs/blocks';
import { PasswordInput } from './PasswordInput';
import { FeatureTable, Preview } from '../../../docs';

<Meta title="Components/Inputs/PasswordInput" component={PasswordInput} />

# Password Input

<br />

## Features

<FeatureTable
  basics={['라벨', 'placeholder', 'register']}
  additionals={[
    'error 필드 메세지',
    'width (Default  400px)',
    'height (Default  56px)',
    '라벨 출력 옵션 (Default  true)',
  ]}
/>

## Props

```tsx
export type PasswordInputProps = {
  label: string;
  placeholder: string;
  register: UseFormRegisterReturn; // react-hook-form
  error?: string;
  width?: string;
  height?: string;
  isLabel?: boolean;
};
```

## PasswordInput Type

### 1. Default

<Preview>
  <PasswordInput
    label="Password"
    placeholder="placeholder"
    register={undefined}
  />
</Preview>

```tsx
<PasswordInput
  label="Password"
  placeholder="placeholder"
  register={register('password', {
    pattern: {
      value: passwordRegex,
      message: '8 자리 이상의 영문 , 숫자 조합의 비밀번호를 입력해주세요.',
    },
    minLength: {
      value: 8,
      message: '8 자리 이상의 영문 , 숫자 조합의 비밀번호를 입력해주세요.',
    },
    required: '비밀번호를 입력해주세요.',
  })}
/>
```

### 2. No Label

<Preview>
  <PasswordInput
    label=""
    isLabel={false}
    placeholder="placeholder"
    register={undefined}
  />
</Preview>

```tsx
<PasswordInput
  label=""
  isLabel={false}
  placeholder="placeholder"
  register={register('password', {
    pattern: {
      value: passwordRegex,
      message: '8 자리 이상의 영문 , 숫자 조합의 비밀번호를 입력해주세요.',
    },
    minLength: {
      value: 8,
      message: '8 자리 이상의 영문 , 숫자 조합의 비밀번호를 입력해주세요.',
    },
    required: '비밀번호를 입력해주세요.',
  })}
/>
```

### 3. Error Message

<Preview>
  <PasswordInput
    label="Password"
    placeholder="placeholder"
    register={undefined}
    error="error message"
/>

</Preview>

```tsx
<PasswordInput
  label="Password"
  placeholder="placeholder"
  register={register('password', {
    pattern: {
      value: passwordRegex,
      message: '8 자리 이상의 영문 , 숫자 조합의 비밀번호를 입력해주세요.',
    },
    minLength: {
      value: 8,
      message: '8 자리 이상의 영문 , 숫자 조합의 비밀번호를 입력해주세요.',
    },
    required: '비밀번호를 입력해주세요.',
  })}
  error="error message"
/>
```
